s = {1, 5, 33, 45, 5, 5, 5}  # set is a collection of non repetive elements.
e = set()     #dont used s = {} as it will create an empty dictionary.
print(s)

#output = {1, 5, 45, 33}  here ordered is not maintain. if we want maintain the order then using list.